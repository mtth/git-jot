name: PR
on:
  pull_request:
    branches:
      - main
jobs:
  lint:
    name: Lint
    steps:
      - name: Check out
        uses: actions/checkout@v4
      - name: Lint
        run: shellcheck git-jot.sh
